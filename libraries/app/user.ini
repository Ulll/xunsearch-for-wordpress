project.name = user
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[guid]
type  = id
index = self

[group_name]
type  = string
index = self

[username]
type  = title
index = both
tokenizer = full

[email]
type  = string
index = self

[main]
type  = body
index = self

[time_created]
type  = numeric
index = self